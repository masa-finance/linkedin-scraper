# LinkedIn Scraper Credentials
# Copy this file to .env and fill in your actual values.
# These are used by the example program in examples/search_profiles/main.go if it's run from the root,
# or if you modify its Load() path.
# Alternatively, set these as actual environment variables.

LI_AT_COOKIE="YOUR_LI_AT_COOKIE_VALUE_HERE"
CSRF_TOKEN="YOUR_CSRF_TOKEN_VALUE_HERE"
JSESSIONID_TOKEN="ajax:YOUR_JSESSIONID_VALUE_HERE" # Ensure to include the 'ajax:' prefix if present